# OpsOS Platform

Mini data + ML + LLM platform (learning project) with production-like engineering standards.

## Dev commands
- `uv run ruff check .`
- `uv run ruff format .`
- `uv run mypy .`
- `PYTHONPATH=services/ingestor/src uv run pytest`
